#Required
    #php #composer #progres_sql #node_js

#connect to database and run "php artisan migrate"

#if run project for the first time run
    # composer require
    # npm install

#start project run
    # php artisan serve
    # npm run watch